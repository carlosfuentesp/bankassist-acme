resources:
  jobs:
    bankassist_evaluation:
      name: bankassist_acme_evaluation
      tasks:
        - task_key: golden_dataset_evaluation
          python_wheel_task:
            package_name: bankassist-acme
            entry_point: bankassist-evaluate
          environment_key: default
      environments:
        - environment_key: default
          spec:
            environment_version: "6"
            dependencies:
              - ../dist/*.whl
              - mlflow>=3.0,<4
