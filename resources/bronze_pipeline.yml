resources:
  pipelines:
    bronze_ingestion:
      name: bankassist_acme_bronze_ingestion

      catalog: ${var.catalog}
      schema: bronze

      serverless: true

      libraries:
        - file:
            path: ../src/ingestion/bronze_structured.py

      configuration:
        structured_source_path: ${var.structured_source_path}