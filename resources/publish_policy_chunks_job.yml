resources:
  jobs:
    publish_policy_chunks:
      name: bankassist_acme_publish_policy_chunks

      tasks:
        - task_key: publish_policy_chunks
          spark_python_task:
            python_file: ../src/documents/publish_policy_chunks.py
          environment_key: default

      environments:
        - environment_key: default
          spec:
            environment_version: "2"