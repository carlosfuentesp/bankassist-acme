resources:
  jobs:
    test_genie_mcp:
      name: bankassist_acme_test_genie_mcp

      tasks:
        - task_key: test_genie_mcp
          notebook_task:
            notebook_path: ../src/agents/bankassist/test_genie_mcp.py
          environment_key: default

      environments:
        - environment_key: default
          spec:
            environment_version: "6"
            dependencies:
              - databricks-langchain==0.20.0