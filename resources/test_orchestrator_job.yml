resources:
  jobs:
    test_orchestrator:
      name: bankassist_acme_test_orchestrator

      tasks:
        - task_key: test_orchestrator
          notebook_task:
            notebook_path: ../src/agents/bankassist/test_orchestrator.py
          environment_key: default

      environments:
        - environment_key: default
          spec:
            environment_version: "6"
            dependencies:
              - databricks-langchain==0.20.0